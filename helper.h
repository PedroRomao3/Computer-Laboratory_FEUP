#ifndef __HELPER_H
#define __HELPER_H

#include <lcom/lcf.h>
#include <stdint.h>

/**
 @brief Prints the binary representation of an 8-bit value.
This function prints the binary representation of an 8-bit value, starting from the most significant bit (MSB).
 @param value The 8-bit value to be printed in binary.
*/
void (printBinary)(uint8_t value);

#endif
